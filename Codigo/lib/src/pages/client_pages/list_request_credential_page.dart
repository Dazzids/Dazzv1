import 'package:dazz/src/models/request_credentials.dart';
import 'package:flutter/material.dart';

class ListRequestedCredentialsPage extends StatelessWidget {
  final List<RequestCredential> list;

  const ListRequestedCredentialsPage({Key key, @required this.list})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('aaa'),
    );
  }
}
